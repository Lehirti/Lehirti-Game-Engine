package lge.state;

/**
 * All classes implementing this interface will be loaded on program startup.
 * put static initializer code allows for modules to be registered
 * automatically.
 * 
 * This is only a tagging interface
 */
public interface StaticInitializer {
  
}
