package org.lehirti.luckysurvivor.inventory;

import org.lehirti.engine.state.IntState;
import org.lehirti.engine.state.Inventory;

public enum Consumable implements Inventory, IntState {
  CUCUMBERS
}
